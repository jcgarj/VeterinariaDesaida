{% extends '::base.html.twig' %}

{% block body -%}
    <section class="posts col-md-9">
        <h1>inventario list</h1>
<div class="table-responsive">
    <table class="table table-bordered records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Cantidadexist</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('inventario_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.cantidadExist }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('inventario_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('inventario_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
    <ul>
        <li>
            <a href="{{ path('inventario_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    </section>
    {% endblock %}
